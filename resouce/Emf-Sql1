	select prod, year , month, avg( X.quant), avg( Y.quant), avg( X.quant)/avg( Y.quant)
	from Sales
	where year= 1997
	group by prod, month, year : X, Y
	such that  X.prod = prod and X.month > month, Y.year = year and Y.month < month
	having  avg( X.quant) > avg( Y.quant) * 2
